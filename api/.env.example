# QueryBase API - Configurações de Ambiente

# Chave de criptografia AES-256-GCM (deve ser a mesma do querybase-web)
# Gere uma nova chave executando: php ../generate-encryption-key.php
QUERYBASE_ENCRYPTION_KEY=
